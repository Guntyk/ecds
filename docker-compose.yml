version: '3.8'

networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "443:443"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
      - '/etc/letsencrypt/live/euro-dance.org:/etc/letsencrypt/live/euro-dance.org:ro'
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  backend:
    build: 
      context: ./server
    networks:
      - dev

  frontend:
    build:
      context: ./client
    networks:
      - dev
